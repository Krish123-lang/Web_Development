/*
function sum(a, b){
    return a+b;
}

let s=sum(4,3)
console.log(s) // 7
*/

// -----------------------------------------

// Arrow Function
const sum = (a,b) => {
    return a+b;
    console.log(`The sum of ${a} and ${b} is: ${a+b}`);
}

s=sum(5,3);
console.log(s); // 8
